doctype html
html(lang="en")
  head
    meta(charset="utf-8")
    title= pageTitle ? pageTitle + ' — ' + title : title
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
  body

    div.container
      p
      form.form-signin(method='post')
        h2.form-signin-heading Register
        label.sr-only(for="inputUserId") Id
        input#inputUserid.form-control(type="text", name="userid", placeholder="Id", required, autofocus)
        label.sr-only(for="inputPassword") Password
        input#inputPassword.form-control(type="password", name="password", placeholder="Password", required)
        button#loginButton.btn.btn-lg.btn-primary.btn-block(type="submit") Register

